# CWIP (Caldera-Wazuh Integration Plugin) Dependencies
# Caldera 기본 의존성은 이미 설치되어 있음 (aiohttp, pyyaml 등)

# Elasticsearch/OpenSearch 클라이언트 (IntegrationEngine용)
opensearch-py>=2.0.0

# 추가 의존성
# aiohttp는 Caldera에 포함됨
# asyncio는 Python 표준 라이브러리
